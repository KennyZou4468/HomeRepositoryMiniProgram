/* pages/add/add.wxss */

/* 页面进入动画 */
@keyframes pageEnter {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.page-enter {
  animation: pageEnter 0.3s ease-out;
}

/* 表单淡入动画 */
@keyframes formSlideUp {
  from {
    opacity: 0;
    transform: translateY(40rpx);
  }

  to {
    opacity: 1;
    transform: translateY(0);
  }
}

.form-animate {
  animation: formSlideUp 0.35s ease-out 0.1s forwards;
  opacity: 0;
}

.container {
  padding: calc(30rpx * var(--ui-scale));
  padding-bottom: calc(200rpx * var(--ui-scale));
  background: linear-gradient(180deg, #fff9f0 0%, #faf8f5 100%);
  min-height: 100vh;
  box-sizing: border-box;
}

/* 页面标题 */
.page-header {
  text-align: center;
  padding: calc(20rpx * var(--ui-scale)) 0 calc(40rpx * var(--ui-scale));
}

.page-title {
  font-size: calc(40rpx * var(--ui-scale));
  font-weight: 600;
  color: #4a4a4a;
  display: block;
}

.page-subtitle {
  font-size: calc(26rpx * var(--ui-scale));
  color: #999;
  margin-top: calc(12rpx * var(--ui-scale));
  display: block;
}

/* 表单样式 */
.form {
  background-color: #ffffff;
  border-radius: 28rpx;
  padding: calc(16rpx * var(--ui-scale)) 0;
  box-shadow: 0 6rpx 24rpx rgba(0, 0, 0, 0.06);
  border: 2rpx solid #f5f0ea;
}

.form-item {
  padding: calc(28rpx * var(--ui-scale)) calc(32rpx * var(--ui-scale));
  border-bottom: 1rpx solid #f5f0ea;
}

.form-item:last-child {
  border-bottom: none;
}

.form-label {
  display: flex;
  align-items: center;
  margin-bottom: calc(18rpx * var(--ui-scale));
  font-size: calc(30rpx * var(--ui-scale));
  color: #5a5a5a;
  font-weight: 500;
}

.label-emoji {
  font-size: calc(32rpx * var(--ui-scale));
  margin-right: calc(12rpx * var(--ui-scale));
}

.required {
  color: #e6995c;
  margin-left: calc(8rpx * var(--ui-scale));
  font-size: calc(26rpx * var(--ui-scale));
}

.optional {
  color: #c0b8ae;
  margin-left: calc(12rpx * var(--ui-scale));
  font-size: calc(24rpx * var(--ui-scale));
  font-weight: 400;
}

.form-input {
  width: 100%;
  height: calc(88rpx * var(--ui-scale));
  padding: 0 calc(28rpx * var(--ui-scale));
  font-size: calc(30rpx * var(--ui-scale));
  color: #5a5a5a;
  background-color: #faf8f5;
  border-radius: 20rpx;
  box-sizing: border-box;
  border: 2rpx solid #f0ebe4;
  transition: border-color 0.2s ease, background-color 0.2s ease, box-shadow 0.2s ease;
}

.form-input:focus {
  border-color: #ffb88e;
  background-color: #fffcf8;
  box-shadow: 0 0 0 4rpx rgba(255, 184, 142, 0.15);
}

.form-input::placeholder {
  color: #c0b8ae;
}

.form-textarea {
  width: 100%;
  min-height: calc(140rpx * var(--ui-scale));
  padding: calc(24rpx * var(--ui-scale)) calc(28rpx * var(--ui-scale));
  font-size: calc(30rpx * var(--ui-scale));
  color: #5a5a5a;
  background-color: #faf8f5;
  border-radius: 20rpx;
  box-sizing: border-box;
  line-height: 1.6;
  border: 2rpx solid #f0ebe4;
}

.form-textarea::placeholder {
  color: #c0b8ae;
}

.char-limit {
  margin-left: auto;
  font-size: calc(24rpx * var(--ui-scale));
  color: #c0b8ae;
  font-weight: normal;
}

/* 可折叠表单项 */
.form-item-collapsible {
  position: relative;
}

.collapse-btn {
  margin-left: auto;
  font-size: 24rpx;
  color: #ff9a6c;
  padding: 4rpx 12rpx;
}

/* 添加字段按钮 */
.add-field-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: calc(20rpx * var(--ui-scale)) calc(32rpx * var(--ui-scale));
  margin: 0 calc(32rpx * var(--ui-scale));
  border-bottom: 1rpx solid #f5f0ea;
}

.add-field-icon {
  font-size: calc(28rpx * var(--ui-scale));
  color: #ff9a6c;
  margin-right: calc(8rpx * var(--ui-scale));
}

.add-field-text {
  font-size: calc(28rpx * var(--ui-scale));
  color: #ff9a6c;
}

/* 按钮区域 */
.button-group {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  display: flex;
  padding: calc(28rpx * var(--ui-scale)) calc(36rpx * var(--ui-scale));
  padding-bottom: calc(calc(28rpx * var(--ui-scale)) + env(safe-area-inset-bottom));
  background-color: #ffffff;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.06);
}

.btn {
  flex: 1;
  height: calc(96rpx * var(--ui-scale));
  line-height: calc(96rpx * var(--ui-scale));
  text-align: center;
  font-size: calc(32rpx * var(--ui-scale));
  border-radius: 48rpx;
  margin: 0;
  padding: 0;
  font-weight: 500;
  transition: transform 0.15s ease, box-shadow 0.15s ease, background-color 0.15s ease;
}

.btn-cancel {
  background-color: #f5f0ea;
  color: #8a8279;
  margin-right: 24rpx;
  flex: 0.6;
}

.btn-cancel:active {
  background-color: #ebe6e0;
  transform: scale(0.96);
}

.btn-save {
  background: linear-gradient(135deg, #ffb88e 0%, #ff9a6c 100%);
  color: #ffffff;
  box-shadow: 0 6rpx 20rpx rgba(255, 152, 108, 0.35);
}

.btn-save:active {
  transform: scale(0.96);
  box-shadow: 0 3rpx 10rpx rgba(255, 152, 108, 0.4);
}

/* 选择器样式 */
.form-select {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: calc(88rpx * var(--ui-scale));
  padding: 0 calc(28rpx * var(--ui-scale));
  font-size: calc(30rpx * var(--ui-scale));
  background-color: #faf8f5;
  border-radius: 20rpx;
  box-sizing: border-box;
  border: 2rpx solid #f0ebe4;
  transition: background-color 0.15s ease, transform 0.15s ease;
}

.form-select:active {
  background-color: #f5f0ea;
  transform: scale(0.99);
}

.select-value {
  flex: 1;
  color: #5a5a5a;
}

.select-placeholder {
  flex: 1;
  color: #c0b8ae;
}

.select-arrow {
  font-size: calc(36rpx * var(--ui-scale));
  color: #c0b8ae;
  margin-left: calc(12rpx * var(--ui-scale));
}

/* 选择弹窗 */
.picker-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: rgba(0, 0, 0, 0.5);
  z-index: 100;
}

.picker-popup {
  position: fixed;
  left: 0;
  right: 0;
  bottom: 0;
  background-color: #ffffff;
  border-radius: 32rpx 32rpx 0 0;
  z-index: 101;
  max-height: 70vh;
  display: flex;
  flex-direction: column;
  padding-bottom: env(safe-area-inset-bottom);
}

.picker-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 32rpx;
  border-bottom: 1rpx solid #f0ebe4;
}

.picker-title {
  font-size: calc(34rpx * var(--ui-scale));
  font-weight: 600;
  color: #4a4a4a;
}

.picker-close {
  width: calc(48rpx * var(--ui-scale));
  height: calc(48rpx * var(--ui-scale));
  line-height: calc(48rpx * var(--ui-scale));
  text-align: center;
  font-size: calc(32rpx * var(--ui-scale));
  color: #999;
}

.picker-list {
  flex: 1;
  max-height: 50vh;
  padding: 16rpx 0;
}

.picker-item {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: calc(28rpx * var(--ui-scale)) calc(32rpx * var(--ui-scale));
  font-size: calc(30rpx * var(--ui-scale));
  color: #5a5a5a;
}

.picker-item-text {
  flex: 1;
}

.picker-item:active {
  background-color: #faf8f5;
}

.picker-item.active {
  color: #ff9a6c;
  background-color: #fff5eb;
}

.picker-item-clear {
  color: #999;
}

.picker-check {
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  text-align: center;
  background: linear-gradient(135deg, #ffb88e 0%, #ff9a6c 100%);
  color: #fff;
  border-radius: 50%;
  font-size: 24rpx;
}

.picker-add {
  padding: 28rpx 32rpx;
  text-align: center;
  border-top: 1rpx dashed #f0ebe4;
}

.picker-add text {
  font-size: 28rpx;
  color: #ff9a6c;
}

.picker-add:active {
  background-color: #fff5eb;
}

.picker-add-input {
  display: flex;
  align-items: center;
  padding: 20rpx 32rpx;
  border-top: 1rpx dashed #f0ebe4;
  gap: 16rpx;
}

.picker-add-input .add-input {
  flex: 1;
  height: 72rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  background-color: #faf8f5;
  border-radius: 12rpx;
  border: 2rpx solid #f0ebe4;
}

.picker-add-input .add-confirm {
  padding: 16rpx 32rpx;
  font-size: 28rpx;
  color: #fff;
  background: linear-gradient(135deg, #ffb88e 0%, #ff9a6c 100%);
  border-radius: 12rpx;
}

/* ========== 暗色主题样式 ========== */
.theme-dark {
  background: linear-gradient(180deg, #1a1a1a 0%, #252525 100%) !important;
}

.theme-dark .page-title {
  color: #f0f0f0 !important;
}

.theme-dark .page-subtitle {
  color: #888888 !important;
}

.theme-dark .form {
  background-color: #2a2a2a !important;
  border-color: #3a3a3a !important;
}

.theme-dark .form-item {
  border-color: #3a3a3a !important;
}

.theme-dark .form-label {
  color: #d0d0d0 !important;
}

.theme-dark .required {
  color: #e6995c !important;
}

.theme-dark .optional {
  color: #666666 !important;
}

.theme-dark .collapse-btn {
  color: #ff9a6c !important;
}

.theme-dark .add-field-btn {
  border-color: #3a3a3a !important;
}

.theme-dark .add-field-icon,
.theme-dark .add-field-text {
  color: #ff9a6c !important;
}

.theme-dark .form-input,
.theme-dark .form-textarea {
  background-color: #333333 !important;
  border-color: #444444 !important;
  color: #e0e0e0 !important;
}

.theme-dark .form-select {
  background-color: #333333 !important;
  border-color: #444444 !important;
}

.theme-dark .select-value {
  color: #e0e0e0 !important;
}

.theme-dark .select-placeholder {
  color: #666666 !important;
}

.theme-dark .count-control {
  background-color: #333333 !important;
  border-color: #444444 !important;
}

.theme-dark .count-btn.decrease {
  background-color: #444444 !important;
  color: #888888 !important;
}

.theme-dark .count-value {
  color: #e0e0e0 !important;
}

.theme-dark .count-unit {
  color: #888888 !important;
}

/* picker弹窗暗色主题 - 直接类名组合 */
.picker-popup.theme-dark {
  background-color: #2a2a2a !important;
}

.picker-popup.theme-dark .picker-header {
  border-color: #3a3a3a !important;
}

.picker-popup.theme-dark .picker-title {
  color: #f0f0f0 !important;
}

.picker-popup.theme-dark .picker-close {
  color: #999999 !important;
  background-color: transparent !important;
}

.picker-popup.theme-dark .picker-list {
  background-color: #2a2a2a !important;
}

.picker-popup.theme-dark .picker-item {
  color: #d0d0d0 !important;
}

.picker-popup.theme-dark .picker-item:active,
.picker-popup.theme-dark .picker-item.selected,
.picker-popup.theme-dark .picker-item.active {
  background-color: #333333 !important;
}

.picker-popup.theme-dark .picker-add {
  border-color: #3a3a3a !important;
  background-color: #2a2a2a !important;
}

.picker-popup.theme-dark .picker-add-input {
  border-color: #3a3a3a !important;
}

.picker-popup.theme-dark .picker-add-input .add-input {
  background-color: #333333 !important;
  border-color: #444444 !important;
  color: #e0e0e0 !important;
}

.picker-popup.theme-dark .picker-item-clear {
  color: #888888 !important;
}

.picker-popup.theme-dark .add-confirm {
  color: #ff9a6c !important;
}

/* 底部按钮栏暗色主题 */
.theme-dark .button-group {
  background-color: #1a1a1a !important;
  box-shadow: 0 -4rpx 20rpx rgba(0, 0, 0, 0.3) !important;
}

.theme-dark .btn-cancel {
  background-color: #333333 !important;
  color: #888888 !important;
}

.theme-dark .btn-cancel:active {
  background-color: #444444 !important;
}
/* ==================== 特殊物品相关样式 ==================== */

/* 特殊物品选项 */
.form-item-special {
  display: flex;
  justify-content: space-between;
  align-items: center;
}

.form-item-special .form-label {
  margin-bottom: 0;
  flex: 1;
}

.form-switch {
  transform: scale(0.9);
}

/* 时间选择器样式 */
.form-picker {
  display: flex;
  align-items: center;
  width: 100%;
  min-height: calc(88rpx * var(--ui-scale));
  padding: 0 calc(28rpx * var(--ui-scale));
  background-color: #faf8f5;
  border-radius: 20rpx;
  font-size: calc(30rpx * var(--ui-scale));
  color: #5a5a5a;
  border: 2rpx solid #f0ebe4;
  box-sizing: border-box;
  transition: background-color 0.15s ease, transform 0.15s ease;
}

.form-picker:active {
  background-color: #f5f0ea;
  transform: scale(0.99);
}

.form-picker .select-placeholder {
  color: #c0b8ae;
}

.form-picker .select-value {
  color: #5a5a5a;
  font-weight: normal;
}

/* 深色模式适配 */
.theme-dark .form-picker {
  background-color: #333333;
  border-color: #444444;
  color: #e0e0e0;
}

.theme-dark .form-picker:active {
  background-color: #3a3a3a;
}

.theme-dark .form-picker .select-placeholder {
  color: #666666;
}

.theme-dark .form-picker .select-value {
  color: #e0e0e0;
}
